# M75-GUI005: Orchestrator A2UI Emission

## Metadata

| Field | Value |
|-------|-------|
| **ID** | M75-GUI005 |
| **Milestone** | M7.5 — Generative Chat UI (GenUI) |
| **Estimate** | 8h |
| **Priority** | P0 |
| **Status** | done |
| **Layer** | backend |

## Description

Teach `GeminiOrchestrator` and `AgentReplyComposer` to build A2UI surfaces from tool results (property search) using catalog-aligned JSON. Prompt-first A2UI blocks alongside conversational text.

## Dependencies

- [M75-GUI004](./m75-gui004.md)

## Traces To

`features/ai_chat/genui_design.md` § Backend Generation

## Acceptance Criteria

- [ ] After `semantic_search`, orchestrator emits `a2ui_surface` with `PropertyCarousel` when listings exist
- [ ] Gemini path receives catalog hint in system context; mock path uses deterministic A2UI builder
- [ ] Only listing IDs from tool results appear in surfaces (no hallucinated properties)
- [ ] Empty search → empathetic text only, no empty carousel

## Affected Files

- `backend/src/infrastructure/ai/gemini-orchestrator.service.ts`
- `backend/src/application/chat/agent-reply-composer.service.ts`
- `backend/src/application/chat/a2ui-surface-builder.service.ts` (new)

## Test Requirements

Unit: surface builder from fixture listings; integration: e2e stream includes `a2ui_surface`

## Definition of Done

- [ ] Search agent turn produces text + A2UI carousel in dev (mock mode)
- [ ] `listing_cards` still emitted for backward compatibility until M75-GUI006
